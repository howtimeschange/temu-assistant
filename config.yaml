temu:
  cdp_port: "9222"
  login_wait_seconds: 40

output:
  desktop: true          # 默认输出到桌面
  data_dir: "data"       # 本地数据目录
  log_dir: "logs"
  keep_days: 30

startup:
  login_wait_seconds: 40  # 首次启动等待登录的秒数
